<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
    {% block stylesheets %}
        <style>
            :root { --bg:#0b0f1a; --card:#141a2b; --text:#e6e9f2; --muted:#b3b9c9; --accent:#5b8cff; }
            * { box-sizing: border-box; }
            body { margin: 0; font-family: Inter, Arial, sans-serif; background: linear-gradient(180deg, #0b0f1a 0%, #0b1222 100%); color: var(--text); }
            a { color: inherit; text-decoration: none; }
            .container { max-width: 1100px; margin: 0 auto; padding: 0 20px; }
            .hero { padding: 90px 0; background: radial-gradient(1200px 600px at 80% -20%, #2b3b70 0%, transparent 60%); }
            .hero__content { display: grid; gap: 30px; grid-template-columns: 1.3fr 0.7fr; align-items: center; }
            .hero h1 { font-size: 44px; margin: 0 0 12px; letter-spacing: 0.2px; }
            .hero p { color: var(--muted); line-height: 1.6; }
            .hero__actions { display: flex; gap: 12px; margin-top: 20px; }
            .hero__card { background: var(--card); padding: 24px; border-radius: 16px; border: 1px solid #26304f; box-shadow: 0 10px 30px rgba(0,0,0,0.25); }
            .hero__badge { display: inline-block; background: var(--accent); color: white; font-weight: 700; padding: 4px 10px; border-radius: 999px; margin-bottom: 10px; }
            .section { padding: 70px 0; }
            .section__header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 24px; }
            .grid { display: grid; gap: 22px; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); }
            .card { background: var(--card); border-radius: 16px; overflow: hidden; border: 1px solid #26304f; display: flex; flex-direction: column; transition: transform .2s ease, box-shadow .2s ease; }
            .card:hover { transform: translateY(-4px); box-shadow: 0 14px 30px rgba(0,0,0,0.28); }
            .card__image { width: 100%; height: 150px; object-fit: cover; background: #0f1424; }
            .card__body { padding: 16px; display: grid; gap: 10px; }
            .card__footer { display: flex; justify-content: space-between; align-items: center; }
            .price { font-weight: 700; color: #9bd1ff; }
            .btn { display: inline-block; padding: 10px 16px; border-radius: 10px; font-weight: 600; transition: transform .15s ease, opacity .15s ease; }
            .btn:hover { transform: translateY(-1px); opacity: 0.95; }
            .btn--primary { background: var(--accent); color: white; }
            .btn--ghost { border: 1px solid #2c3a63; color: #cbd3ea; }
            .btn--small { padding: 6px 12px; font-size: 14px; background: #1f2a4a; }
            .link { color: #9bd1ff; }
            .topbar { display: flex; justify-content: flex-end; padding: 12px 0; }
            .lang { display: flex; gap: 8px; }
            .lang a { padding: 6px 10px; border: 1px solid #2c3a63; border-radius: 8px; font-size: 13px; transition: background .2s ease; }
            .lang a:hover { background: #1a2340; }
            .navbar { border-bottom: 1px solid #1f2a4a; padding: 16px 0; position: sticky; top: 0; backdrop-filter: blur(6px); background: rgba(11,15,26,0.7); z-index: 10; }
            .navbar__inner { display: flex; justify-content: space-between; align-items: center; }
            .logo { font-weight: 800; letter-spacing: 0.5px; }
            .nav { display: flex; gap: 18px; color: var(--muted); }
            .nav a:hover { color: var(--text); }
            .nav .logout-button { background: none; border: none; padding: 0; margin: 0; cursor: pointer; color: inherit; font: inherit; }
            .nav .logout-button:hover { color: var(--text); }
            .footer { border-top: 1px solid #1f2a4a; padding: 40px 0; margin-top: 60px; color: var(--muted); }
            .footer__inner { display: flex; justify-content: space-between; gap: 20px; flex-wrap: wrap; }
        </style>
    {% endblock %}
</head>
<body>
<div class="container topbar">
    <div class="lang">
        <a href="{{ path('lang_switch', { _locale: 'pl' }) }}">PL</a>
        <a href="{{ path('lang_switch', { _locale: 'en' }) }}">EN</a>
    </div>
</div>

<header class="navbar">
    <div class="container navbar__inner">
        <div class="logo">CourseLab</div>
        <nav class="nav">
            <a href="{{ path('home') }}">{{ 'nav.home'|trans }}</a>
            <a href="{{ path('courses_index') }}">{{ 'nav.courses'|trans }}</a>

            {% if is_granted('ROLE_ADMIN') %}
                <a href="/admin">{{ 'nav.admin'|trans }}</a>
            {% endif %}

            {% if app.user %}
                <a href="{{ path('app_profile') }}">Profile</a>
                <form method="post" action="{{ path('app_logout') }}" style="display:inline">
                    <input type="hidden" name="_token" value="{{ csrf_token('logout') }}">
                    <button type="submit" class="link logout-button">Logout</button>
                </form>
            {% else %}
                <a href="{{ path('app_login') }}">Login</a>
                <a href="{{ path('app_register') }}">Register</a>
            {% endif %}
        </nav>
    </div>
</header>

{% block body %}{% endblock %}

<footer class="footer">
    <div class="container footer__inner">
        <div>
            <strong>CourseLab</strong><br>
            {{ 'footer.tagline'|trans }}
        </div>
        <div>
            {{ 'footer.contact'|trans }}: contact@courselab.test
        </div>
    </div>
</footer>
</body>
</html>
